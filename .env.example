# Database Configuration (already used by Drizzle config and postgres.ts)
DB_HOST=localhost
DB_PORT=5432
DB_USER=rpgf_user
DB_PASSWORD=rpgf_password
DB_NAME=rpgf_db

# JWT Configuration for SIWE
JWT_SECRET= # Replace with a long, random, secure string
JWT_EXPIRATION_MINUTES=60

# SIWE Configuration (Optional, SiweMessage can infer some)
# SIWE_DOMAIN=yourdomain.com
# SIWE_STATEMENT="Sign in to Drips RetroPGF"
